<% include partials/header %>
<div class="container">
   <header class="jumbotron">
      <div class="container">
         <h1>Innovation Sites</h1>
         <p>View the following leading Innovation Sites</p>
         <p>
            <a class="btn btn-primary btn-lg" href="/launchsite/new">Add New Launchsite</a>
         </p>
      </div>
   </header>
   
   <div  class="row text-center" style="display:flex; flex-wrap: wrap;">
      <% launchsite.forEach(function(launchsite){ %>
         <div class="col-md-3 col-sm-6">
            <div class="thumbnail">
               <img src="<%= launchsite.image %>">
               <div class="caption">
                  <h4><%= launchsite.name %></h4>
               </div>
            </div>
         </div>
      <% }); %>
   </div>
   
</div>

<% include partials/footer %>